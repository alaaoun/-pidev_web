{% extends 'home/index.html.twig' %}

{% block title %}New Reservation{% endblock %}

{% block body %}
<!-- Detail Start -->
<div class="container-fluid pt-5">
    <div class="container pt-5 pb-3">
       
        <div class="row align-items-center pb-2">
            <div class="col-lg-6 mb-4">
<h1 class="text-uppercase mb-5" id="scooterid"> E.scooter   {{ idTrotinette }}
 
            <div class="col-lg-6 mb-4">
                   <img class="img-fluid mb-4" src="{{ asset('img/tt7.png') }}" alt="">    
                   
<h3>10 TND/HOUR</h3>      
              </div>


                <h4 class="mb-2" id="scooterPrice"></h4> <!-- Prepared to dynamically update text content -->
            </div>
        </div>
    </div>
</div>
<!-- Detail End -->

{{ include('resrvation/_form.html.twig' ,  {'button_label': 'Reserve Now'}) }}
{% block javascripts %}
<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/js/bootstrap.bundle.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-datepicker/1.9.0/js/bootstrap-datepicker.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/moment.js/2.29.1/moment.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/tempusdominus-bootstrap-4/5.39.0/js/tempusdominus-bootstrap-4.min.js"></script>
<script>
$(document).ready(function() {
    // Debug: Check if jQuery is loaded
    console.log("jQuery version:", $.fn.jquery);

    const customModal = $('#customModal');
    const closeBtn = $('.close-btn');

    // Debug: Log the elements found
    console.log("Custom Modal found:", customModal.length);
    console.log("Close Button found:", closeBtn.length);

    $(closeBtn).click(function(event) {
        console.log("Close button clicked.");  // Debug: Confirm click handling
        customModal.addClass('hidden'); // This will hide the modal when the close icon is clicked
         location.reload(); // Reload the page
          // Prevent any default action that might be triggered
    });

    // Other existing script content...
});
</script>


<script>
document.querySelector('#modelInput');
document.querySelector('#priceInput');
document.addEventListener('DOMContentLoaded', function() {
    

    var modelInput = document.getElementById('modelInput');
    var priceInput = document.getElementById('priceInput');

  

    var scooterName = sessionStorage.getItem('scooterName');
    var scooterPrice = sessionStorage.getItem('scooterPrice');

   

    if (modelInput && scooterName) {
        modelInput.value = scooterName;
        
    } else {
        
    }

    if (priceInput && scooterPrice) {
        priceInput.value = scooterPrice;
        console.log('priceInput value set:', priceInput.value);
    } else {
        
    }
});


</script>



 {% endblock %}
{% endblock %}
